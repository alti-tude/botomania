#ifndef UTILS_H
#define UTILS_H
#include<bits/stdc++.h>
using namespace std;

vector<pair<int, int> > valid_moves(int* grid, int player);
void flip(int* grid, pair<int, int> mov, int player);

#endif
